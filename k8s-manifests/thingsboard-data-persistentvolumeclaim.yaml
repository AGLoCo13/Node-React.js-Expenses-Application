apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: thingsboard-data
  name: thingsboard-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
