apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: node-red-data
  name: node-red-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
