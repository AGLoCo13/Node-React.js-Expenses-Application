apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Observability stack (task S4). Χειρόγραφα manifests, όχι kube-prometheus-stack:
# ο operator + τα CRDs του δεν χωράνε στον 2-vCPU κόμβο και το roadmap τα βάζει
# ρητά σε "μελλοντικές επεκτάσεις".
resources:
  - kube-state-metrics.yaml
  - prometheus.yaml
  - grafana.yaml
