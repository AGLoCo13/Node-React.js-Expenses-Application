apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: urbansync-prod
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/AGLoCo13/Node-React.js-Expenses-Application.git
    targetRevision: dev-combined
    path: urbansync-v2/k8s/overlays/prod
  destination:
    server: https://kubernetes.default.svc
    namespace: urbansync
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
    syncOptions:
      - CreateNamespace=true
  # urbansync-secrets is created by the CSI driver at pod start time, not from Git.
  # ignoreDifferences prevents ArgoCD from pruning it.
  ignoreDifferences:
    - group: ""
      kind: Secret
      name: urbansync-secrets
      namespace: urbansync
      jsonPointers:
        - /data
        - /metadata/annotations
